软件PWM用定时器中断实现，需要考虑中断时间，可以映射到所有IO口，并且不需要考虑硬件，只要有定时器即可
硬件PWM用硬件PWM实现，没有中断，只能映射到一部分规定的IO口，需要考虑硬件，必须有硬件PWM的型号才能用

PPM是从软件PWM基础上改的，优劣处同软件PWM，另外，通道3的信号总是有点问题，信号偏高，建议用作油门杆

sbus是直接用串口1发送的，并没有取反，严格来说不是sbus信号，外部要接一个取反电路。
但基本编码已经完成